"use client";
import { FormEvent, useState } from "react"; import Link from "next/link"; import { BookOpen, CheckCircle2, ShieldCheck } from "lucide-react"; import { createSupabaseBrowserClient } from "@/lib/supabase";
export default function Login(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const supabase=createSupabaseBrowserClient();if(!supabase){setMessage("Mode démo : configurez Supabase dans .env.local pour activer la connexion.");return}const {error}=await supabase.auth.signInWithPassword({email:String(form.get("email")),password:String(form.get("password"))});setMessage(error?error.message:"Connexion réussie.")}return <main className="form-shell"><section className="form-aside"><span className="kicker gold">ESPACE STAGIAIRE</span><h1>Votre apprentissage, au même endroit.</h1><p>Retrouvez vos cours, supports, résultats et attestations dans un espace privé.</p><div className="form-points"><span><BookOpen/> Cours accessibles 24h/24</span><span><CheckCircle2/> Progression enregistrée</span><span><ShieldCheck/> Données protégées</span></div></section><section className="form-main"><form className="form-card" onSubmit={submit}><Link href="/" className="brand"><span className="brand-mark">L</span><span><strong>LIDA</strong><small>FORMATION</small></span></Link><br/><h2>Bon retour parmi nous</h2><p>Connectez-vous pour continuer votre formation.</p><div className="form-group"><label>Email</label><input name="email" type="email" required placeholder="vous@exemple.com"/></div><br/><div className="form-group"><label>Mot de passe</label><input name="password" type="password" required placeholder="Votre mot de passe"/></div><button className="button primary" type="submit">Se connecter</button>{message&&<p className="form-note">{message}</p>}<p className="form-note">Pas encore inscrit ? <Link href="/inscription">Demander une inscription</Link></p><p className="form-note"><Link href="/dashboard">Voir le dashboard de démonstration</Link></p></form></section></main>}
